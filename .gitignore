node_modules/
dist/
dist-ssr/
# Service worker output from `vite dev`.
dev-dist/

# Local Firebase credentials. The deployed build reads .env.production, which is
# committed on purpose; these local overrides are not.
.env
.env.local
.env.*.local

*.log
npm-debug.log*
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Machine-local Claude Code permissions, not shared config.
.claude/settings.local.json
